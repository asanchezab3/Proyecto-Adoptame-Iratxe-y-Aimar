import Menu from "../componentes/Menu";

const Contacto = () => {
    return (
        <div>
            <Menu />
            <p>Esta es la página de contacto</p>
        </div>
    );
};

export default Contacto;