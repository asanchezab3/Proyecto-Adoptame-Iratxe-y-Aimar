import Menu from "../componentes/Menu";

const Lista = () => {
    return (
        <div>
            <Menu />
            <p>Esta es la página de la lista de animales</p>
        </div>
    );
};

export default Lista;